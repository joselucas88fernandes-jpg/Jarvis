# Technical Audit & Mapping Report: MARK XXXIX (39)

## 1. AUDIO & INPUT STT
- **Current Microphone Loop**:
    - **Location**: `main.py` -> `JarvisLive._listen_audio()`
    - **Technology**: Uses `sounddevice` and `numpy`. Implements software gain boost (`1.8x`) and an adaptive VAD (Voice Activity Detection) gate.
- **Speech-to-Text (STT)**:
    - **Implementation**: Currently handled **remotely** via the Gemini Live API (`models/gemini-2.5-flash-native-audio-preview-12-2025`).
- **Wake-Word Detection**:
    - **Status**: **Legacy / Disconnected**.
    - **Note**: A "wake_word" variable and UI input exist in `ui/tabs/settings_tab.py` (default: "jarvis"), but the current `main.py` implementation is an "always-on" stream when unmuted.
- **Planned Shift**: Transitioning to local Push-to-Talk (PTT) with `faster-whisper` for local STT.

## 2. UI & SIDEBAR TABS
- **Navigation Framework**: Defined in `ui/ui_main.py` via `MainWindow` using a `QStackedWidget` for tab switching.
- **Tab Mapping**:
    - **Início (MAIN)**: `ui/tabs/main_tab.py`. Contains the core HUD (`HudCanvas`), system monitors (CPU/GPU/RAM), and the software audio visualizer.
    - **Memórias (MEM)**: `ui/tabs/memory_tab.py`. Features a force-directed graph visualization of memories.
    - **Projetos (PROJ)**: `ui/tabs/projects_tab.py`. A specialized file manager and code editor targeting the `./projects` workspace.
    - **Logs (LOGS)**: `ui/tabs/logs_tab.py`. Real-time polling of `autogpt_audit.log` and `system.log`.
    - **Configurações (CFG)**: `ui/tabs/settings_tab.py`. Handles API keys, audio thresholds, and autonomous engine parameters.

## 3. PENDING/DANGLING FUNCTIONS
- **Tool Logic Redundancy**:
    - Significant duplication between `_execute_tool` in `main.py` and `_call_tool` in `agent/executor.py`. Both maintain manual mappings of strings to functions.
- **Autonomous Engine**:
    - `agent/autogpt_runner.py` and `agent/task_queue.py` are largely disconnected from the primary `main.py` loop. The runner includes "learning" capabilities for GUI interaction that are currently unused.
- **Memory Backend Mismatch**:
    - The UI graph (`memory_tab.py`) pulls from `memory/local_vectordb.py` (LanceDB), but the active agent in `main.py` primarily updates a flat JSON file via `memory/memory_manager.py`.
- **Advanced File Processing**:
    - `actions/file_processor.py` contains sophisticated logic for media and document handling that is partially shadowed by simpler implementations in `main.py`.

## 4. ECOSYSTEM COMPATIBILITY
- **Audio/Vision**: `sounddevice`, `opencv-python`, `mss`, `pyautogui`.
- **AI/LLM**: `google-genai` (Primary). `ollama` is present in `requirements.txt` but lacks an active bridge in the current code.
- **Database**: `lancedb` (Local Vector DB) and `pyarrow`.
- **System/Automation**: `playwright`, `pywinauto`, `psutil`.

---
*Report generated by Principal Software Architect Jules.*
